<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animalsafetyv1">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Animal Safety"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity android:name=".RegistroPropietarioMascota3"></activity>
        <activity android:name=".RegistroPropietarioMascota2" />
        <activity android:name=".ListaMascotasAdoptadas" />
        <activity android:name=".RegistroSeguimiento" />
        <activity android:name=".RegistroMascotaParticular" />
        <activity android:name=".RegistroIntegrante" />
        <activity android:name=".AsociarUsuario" />
        <activity android:name=".RegistroPropietarioMascota" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>